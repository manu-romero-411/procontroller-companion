[general]
; Command run when the Pro Controller's Home button is pressed twice in
; quick succession. Passed verbatim to `bash -c "<command>"`, so quoting,
; spaces, pipes, redirects and env vars follow normal bash conventions.
;
; Example:
home_launch = notify-send "Pro Controller" 'home button double-pressed'
